#include <std.h>
#include <tensor.h>

int main() {
    // Create a tensor of integers
    Tensor<int> myTensor(16, 16);

    myTensor.init();
    // Display the tensor
    myTensor.display();

    return 0;
}
